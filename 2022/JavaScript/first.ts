import * as readlineSync from 'readline-sync';

function findElfWithMostCalories(calories: string): number {
  const caloriesForElf: string[] = calories.split('\n\n');
  console.log(caloriesForElf);
  return 1;
}
const userInput: string = readlineSync.question('input: ');

